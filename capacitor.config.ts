import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.PANDROLRAIPUR.app',
  appName: 'pandrol',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
